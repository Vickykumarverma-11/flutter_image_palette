import 'package:flutter/material.dart';

class ImagePageConstants {
  // Sizes
  static const double borderRadius = 12.0;
  static const double padding = 16.0;
  static const double iconSize = 64.0;
  static const double errorIconSize = 80.0;

  // Durations
  static const Duration animationDuration = Duration(milliseconds: 400);
  static const Duration switcherDuration = Duration(milliseconds: 300);

  // Button Colors
  static const Color buttonColor = Color(0xFF00BCD4);
  static const Color buttonPlunkColor = Color(0xFF0097A7);
  static const Color buttonShadowColor = Color(0xFF006064);
}
